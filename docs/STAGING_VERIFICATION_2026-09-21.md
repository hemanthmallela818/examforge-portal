# Connected staging verification — 21 September 2026

Target: `jee-staging` (`vjynyashxomyryqjfkas`). Production was not modified.

## Initial live 80-candidate rehearsal: core paths passed; provisioning gap found

The connected harness completed with exit code 0. It obtained credentials internally from the authenticated Supabase CLI, created a random-password temporary administrator, verified TOTP/AAL2 through Auth, and revoked/deleted that administrator afterward. No credentials were printed or saved.

- 80 students created, authenticated and claimed signed sessions.
- 80 Realtime subscriptions, exam starts and autosaves succeeded.
- 80 submissions and 80 idempotent submission retries succeeded.
- Exactly 80 distinct candidates and 80 distinct expected scores, from -75 to 320; zero remaining active sessions.
- Answer keys were hidden; cross-account sessions/results were inaccessible.
- Forged result writes, exam creation and student provisioning were blocked.
- The Edge Function returned a student ID, but a subsequent database audit found that its stale staging version had not created the student/profile rows. The harness was strengthened to reject this false-positive response. The current local function already contains the required atomic finalization and rollback handling.
- No lost, duplicated or cross-account grading was detected.

This is an 80-candidate functional/concurrency rehearsal with paced authentication and 16 concurrent start/save/submit workers, not evidence for 1,000 simultaneous users or sustained-load latency targets.

## Corrected live rehearsal: PASS

The stale staging Edge Function was replaced with the repository's hardened `manage-student` implementation (staging version 6). The entire 80-candidate rehearsal then passed again, including the new checks for a persistent student record, exact class/section assignment, and student role. The temporary MFA administrator was revoked and deleted. Production was unchanged.

The local app now uses staging via ignored `.env.local`, with a publishable frontend key only. Staging CORS permits the exact localhost/127.0.0.1 development and preview origins on ports 5173 and 4173.

Repository verification on 21 September: 242 tests passed; changed operational scripts passed ESLint; the production bundle built successfully.

## Private Storage recovery: PASS

A unique temporary 68-byte PNG in the private `exam-assets` bucket was exported, removed, restored and checked by SHA-256. Anonymous access was blocked; signed access returned the correct bytes. The test object was removed afterward. This verifies the object recovery mechanism, not a full production backup policy or measured whole-system RPO/RTO.

## Staging logical database recovery: PASS

The populated staging `public`, `auth`, and `storage` logical schema/data backup was restored into a uniquely named database in the local Supabase container. All 12 application tables matched exact row counts and content digests, including all 80 results. Zero unvalidated public constraints remained. The source was unchanged across export. The complete proof took 26.612 seconds; local restore plus verification took 4.135 seconds. Temporary database and sensitive dump files were deleted afterward.

These are measured logical-recovery timings for this small test dataset, not a hosted production RTO, a PITR guarantee, or proof at campus scale. The first comparison failed because JSON property ordering differed; comparison now checks each named table's count and digest independently.

## Cleanup

The two initial identified rehearsal datasets, including the orphan Auth user created by the stale function, were removed transactionally. The successful rerun's 81 test students, 80 results, exam and class were also removed after recovery verification. The existing non-test administrator and audit events were retained. Fixture deletion is not undoable from a retained dump (temporary dumps were intentionally removed); fixtures can be regenerated by rerunning the harness. No production data was touched.

## Repeatable commands

With Supabase CLI authentication already configured, set `REHEARSAL_CONFIRM_DISPOSABLE` to `YES_RESET_THIS_DISPOSABLE_PROJECT_AFTER_REHEARSAL`, then run:

```powershell
npm run test:rehearsal:staging
npm run ops:prove-storage-restore:staging
npm run ops:prove-restore:staging
```

All three commands are restricted to the exact staging project above. The rehearsal intentionally leaves candidate fixtures and immutable results for verification; an operator must perform bounded disposable-fixture cleanup afterward. Never run result-retention cleanup against production.

## Remaining release evidence

The corrected 100-candidate run is the next validation-window gate; the earlier 100-candidate attempt exposed a harness scoring assumption and was cleaned up. Current-working-tree hosted CI, real-network browser resilience, peak/soak capacity, hosted disaster-recovery/PITR validation, and assisted screen-reader acceptance remain outstanding. The operator drill itself now passes. Leaked-password protection remains disabled in staging. Sentry remains deferred at the user's request. No production deployment or production approval is claimed.

A malformed bare secret-key line was removed from the ignored `.env` file while preserving its valid assignments. The CLI printed that line in a configuration error before it was corrected. That exposed key must be revoked/rotated before real use; no key is included in this report.
